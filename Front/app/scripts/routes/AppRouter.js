/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var AppRouterRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return AppRouterRouter;
});
